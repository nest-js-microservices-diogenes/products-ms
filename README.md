# Products Microservice

# Dev

1. Clonar el repositorio
2. Instalar dependencias
3. Crear un ```.env``` basado en el ```.env.example```
4. Ejecutar la migracion de prisma ```npx prisma migrate dev```
5. Ejecutar ```pnpm run start:dev```